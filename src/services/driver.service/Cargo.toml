[package]
name = "driver"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "entry"
path = "src/entry.rs"

[dependencies]
swiftlib = { path = "../../user", features = ["std-support"] }

[profile.release]
panic = "abort"
lto = true

[profile.dev]
panic = "abort"
