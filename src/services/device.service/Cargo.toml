[package]
name = "device"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "entry"
path = "src/entry.rs"

[dependencies]
mochi_syscall = { path = "../../user", features = ["std-support"] }

[profile.release]
panic = "abort"
lto = true

[profile.dev]
panic = "abort"

