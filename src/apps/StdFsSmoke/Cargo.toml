[package]
name = "stdfssmoke"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
mochi_syscall = { path = "../../user", features = ["std-support"] }

[profile.release]
panic = "abort"

[profile.dev]
panic = "abort"

