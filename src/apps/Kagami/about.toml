name = "Kagami"
bundle_id = "com.mochi.kagami"
version = "0.1.0"
developer = "tas0dev"
entry = "entry.elf"
description = "Kagami is a window server"

# build.rs (builders/apps.rs) 用: Cargo の bin / feature 指定
cargo_bin = "kagamid"
cargo_no_default_features = true
cargo_features = ["mochios", "backend-mochios-vga"]
