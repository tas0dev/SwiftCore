[package]
name = "terminal"
version = "0.1.0"
edition = "2024"

[dependencies]
swiftlib = { path = "../../user", features = ["std-support"] }

[profile.release]
panic = "abort"

[profile.dev]
panic = "abort"
